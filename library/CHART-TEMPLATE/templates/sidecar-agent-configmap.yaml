---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "common.naming.fullname" . }}-agent-config
data:
  newrelic-infra.yml: |
    {{- fromYaml (include "common.agentConfig.defaults" . ) | mustMergeOverwrite .Values.agent | toYaml | nindent 4}}
