
{{- if not (lookup "v1" "Namespace" "" .Values.config.subAgentsNamespace) -}}
apiVersion: v1
kind: Namespace
metadata:
  annotations:
    "helm.sh/resource-policy": keep
  name: {{ .Values.config.subAgentsNamespace }}
{{- end -}}
