{{- $subAgentsNamespace := index (include "newrelic-agent-control.config.content" . | fromYaml) "k8s" "namespace_agents" -}}
{{- if not (lookup "v1" "Namespace" "" $subAgentsNamespace) -}}
apiVersion: v1
kind: Namespace
metadata:
  annotations:
    "helm.sh/resource-policy": keep
  name: {{ $subAgentsNamespace }}
{{- end -}}
