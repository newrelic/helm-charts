{{- if not .Values.ksm.enabled }}
KSM Warning
===========
You have not enabled the KSM dependency, which is required for nri-kubernetes
to be able to fetch data. Please ensure that you either have KSM already
deployed in your cluster, or enable the KSM dependency in this chart by setting
`Values.ksm.enabled` to `true` in your values.yaml file.
{{- end }}
