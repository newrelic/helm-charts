apiVersion: v1
kind: Service
metadata:
  name: otel-deployment-collector
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: {{ include "nrKubernetesOtel.deployment.fullname" . }}
  ports:
    - name: otlp-http
      port: 4318
      targetPort: 4318
    - name: otlp-grpc
      port: 4317
      targetPort: 4317
