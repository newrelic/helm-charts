apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "simple_busybox.fullname" . }}-test-connection"
  labels:
    {{- include "simple_busybox.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "simple_busybox.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
