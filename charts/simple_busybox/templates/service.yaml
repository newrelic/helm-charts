apiVersion: v1
kind: Service
metadata:
  name: {{ include "simple_busybox.fullname" . }}
  labels:
    {{- include "simple_busybox.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "simple_busybox.selectorLabels" . | nindent 4 }}
