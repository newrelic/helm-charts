{{- include "mssql-otel.validate.mssql" . -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "mssql-otel.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "mssql-otel.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "mssql-otel.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "mssql-otel.labels" . | nindent 8 }}
    spec:
      containers:
        - name: mssql-otel
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          args:
            - --config=/etc/mssql-otel/config.yaml
          env:
            - name: MSSQL_USERNAME
              valueFrom:
                secretKeyRef:
                  name: {{ include "mssql-otel.credentials.secretName" . }}
                  key: username
            - name: MSSQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "mssql-otel.credentials.secretName" . }}
                  key: password
            - name: NEW_RELIC_LICENSE_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "newrelic.common.license.secretName" . }}
                  key: {{ include "newrelic.common.license.secretKeyName" . }}
          {{- if .Values.resources }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          {{- end }}
          volumeMounts:
            - name: config
              mountPath: /etc/mssql-otel
              readOnly: true
      volumes:
        - name: config
          configMap:
            name: {{ include "mssql-otel.fullname" . }}-config
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
