{{- if .Values.agentControlCd.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "agent-control-cd.secret.name" . }}
  namespace: {{ .Release.Namespace }}
type: Opaque
data:
  agent-control-flux.yaml: {{ .Values.agentControlCd.chartValues | toYaml | b64enc }}
{{- end }}
