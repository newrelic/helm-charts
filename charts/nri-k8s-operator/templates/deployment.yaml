apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "nri-k8s-operator.fullname" . }}
  labels:
    {{- include "nri-k8s-operator.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicas }}
  selector:
    matchLabels:
      {{- include "nri-k8s-operator.appLabel" . | nindent 6 }}
  template:
    metadata:
      {{- if .Values.podAnnotations }}
      annotations:
        {{- toYaml .Values.podAnnotations | nindent 8 }}
      {{- end }}
      labels:
        {{- include "nri-k8s-operator.labels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ template "nri-k8s-operator.serviceAccountName" . }}
      {{- if .Values.podSecurityContext.enabled }}
      securityContext:
        runAsUser: {{ .Values.podSecurityContext.runAsUser }}
        runAsGroup: {{ .Values.podSecurityContext.runAsGroup }}
        fsGroup: {{ .Values.podSecurityContext.fsGroup }}
      {{- end }}
      {{- if .Values.image.pullSecrets }}
      imagePullSecrets:
        {{- toYaml .Values.image.pullSecrets | nindent 8 }}
      {{- end }}
      containers:
      - name: {{ template "nri-k8s-operator.name" . }}
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        imagePullPolicy: "{{ .Values.image.pullPolicy }}"
        env:
        - name: clusterName
          value: {{ include "nri-k8s-operator.cluster" . }}
        volumeMounts:
        - name: config
          mountPath: /etc/newrelic/newrelic-infra-operator/
        - name: tls-key-cert-pair
          mountPath: /tmp/k8s-webhook-server/serving-certs/
        readinessProbe:
          httpGet:
            path: /healthz
            port: 9440
          initialDelaySeconds: 1
          periodSeconds: 1
        {{- if .Values.resources }}
        resources:
          {{- toYaml .Values.resources | nindent 10 }}
        {{- end }}
      volumes:
      - name: config
        configMap:
          name: {{ template "nri-k8s-operator.fullname" . }}-config
      - name: tls-key-cert-pair
        secret:
          secretName: {{ template "nri-k8s-operator.fullname" . }}-admission
      {{- if $.Values.priorityClassName }}
      priorityClassName: {{ $.Values.priorityClassName }}
      {{- end }}
      {{- if $.Values.nodeSelector }}
      nodeSelector:
        {{- toYaml $.Values.nodeSelector | nindent 8 }}
      {{- end }}
      {{- if .Values.tolerations }}
      tolerations:
        {{- toYaml .Values.tolerations | nindent 8 }}
      {{- end }}
      {{- if .Values.affinity }}
      affinity:
        {{- toYaml .Values.affinity | nindent 8 }}
      {{- end }}
