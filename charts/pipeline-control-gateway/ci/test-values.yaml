global:
  cluster: sa-cluster
  # Replaced in CI with a real license key from a GitHub Actions secret (see
  # lint_test_charts.yaml) because the collector now validates the key on
  # startup and a dummy value fails readiness during `ct install`.
  licenseKey: REPLACE_WITH_REAL_LICENSE_KEY
