apiVersion: v1
kind: Secret
metadata:
  name: ac-deployment-secret
  namespace: {{ .Release.Namespace }}
type: Opaque
data:
  # Encode your values in base64
  agent-control-deployment.yaml: {{ .Values.agentControlDeployment | toYaml | b64enc }}
