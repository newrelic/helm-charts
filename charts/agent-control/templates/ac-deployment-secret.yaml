{{- if .Values.agentControlDeployment.enabled -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "agent-control-deployment.secret.name" . }}
  namespace: {{ .Release.Namespace }}
type: Opaque
data:
  agent-control-deployment.yaml: {{ include "agent-control.agent-control-deployment.config" . }}
  global.yaml: {{ .Values.global | toYaml | b64enc }}
{{- end -}}
